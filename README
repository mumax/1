Required software/libraries:

* Subversion/dynamag access:
install seahorse, make keypair for dynamag login
simlink /var/www/user
sftp access to sim/$user, home: add entry to "places" in dolphin
install kdesvn

* Git-svn
install git-svn gitk
git svn clone -s svn+ssh://dynamag.ugent.be/home/sim/svn/multiscale .
git svn rebase
git diff --color
git add ...
git commit -a
git svn dcommit
git svn rebase

* Git
git clone dynamag.ugent.be/home/arne/git .
git branch -a
git checkout -b ben-svn origin/ben-svn
git checkout -b arne origin/arne

* CUDA
cuda 3.0 + devdriver
compute-exclusive mode?
export PATH=$PATH:/usr/local/cuda/bin
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/cuda/lib64:/usr/local/cuda/lib
export C_INCLUDE_PATH=$C_INCLUDE_PATH:/usr/local/cuda/include
export CPLUS_INCLUDE_PATH=$CPLUS_INCLUDE_PATH:/usr/local/cuda/include

* Go
install python-setuptools python-dev build-essential gcc bison libc6-dev ed gawk make mercurial
export GOROOT=$HOME/sbin/go
export GOOS=linux
export GOARCH=amd64
export GOBIN=$HOME/sbin
hg clone -r release https://go.googlecode.com/hg/ $GOROOT
./all.bash
sudo ./sudo.bash
sudo cp sbin/go/misc/kate/go.xml /usr/share/kde4/apps/katepart/syntax/


* env. variables:
edit .bashrc
export PATH=$PATH:/usr/local/cuda/bin:/home/XXX/YYY/trunk/bin
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/cuda/lib64:/usr/local/cuda/lib

* Compiling FFTW:
./configure CFLAGS="-fPIC" --enable-float  --enable-threads --enable-sse
make
sudo make install

* Dependencies for scripts etc:
doxygen ffmpeg gnuplot mencoder octave


(* general Ubuntu)
software sources: enable partner repo
download from main server

sudo apt-get install ubuntu-restricted-extras flashplugin sun-java6-plugin sun-java6-jdk firefox thunderbird
purge openjdk

automount win partition:
/etc/fstab:  /dev/sdaXXX /media/xp ntfs-3g defaults,locale=en_US.utf8 0 0
ln -s /media/xp /home/XXX/My\ Documents

purge network-manager-kde
install network-manager-gnome

install network-manager-vpnc
nm-applet: add cisco compatible
Gateway: rtrvpn.ugent.be
Group name: ipsecclient
Group password: cisco123
Encryption: Secure (Default)
NAT traversal: Disabled

UGent WPA:
SSID: UGentWPA
Mode: Infrastructure
Security: WPA & WPA2 Enterprise
Authentication: Tunneled TLS
CA certificate: /etc/ssl/certs/GTE_CyberTrust_Global_Root.pem
Inner authentication: MSCHAPv2

eduroam:
SSID: eduroam
Mode: Infrastructure
Security: WPA & WPA2 Enterprise
Authentication: Tunneled TLS
CA certificate: /etc/ssl/certs/GTE_CyberTrust_Global_Root.pem
Inner authentication: MSCHAPv2

/etc/default/grub:
quiet (remove splash)
3s timeout

H:\ schijf:
smb://avsteenk@samba1.ugent.be/avsteenk/
(add dolphin bookmark H-schijf)





